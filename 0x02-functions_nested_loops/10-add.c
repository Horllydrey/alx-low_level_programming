#include "main.h"
/**
 * add - it adds two integer
 * Description: It adds two integer and prints the result
 * @num1: a parameter to be checked
 * @num2: second parameter
 * Return: 0 if successful
 */
int add(int num1, int num2)
{
	return(num1 + num2);
}
