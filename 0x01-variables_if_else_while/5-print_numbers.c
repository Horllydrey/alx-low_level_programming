#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: prints digits 0 - 9
 *
 * Return: Always 0 (success)
 */

int main(void)
{
int i;
for (i = 0; i < 10; i++)
}
printf("%d", i);
return (0);
}
