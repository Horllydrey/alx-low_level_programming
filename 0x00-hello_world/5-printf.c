#include <stdio.h>
/* main - Entry point
 * Description: a function that prints a sting follwed by a new line using str
 * Return: 0 if successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
